
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 👇 TEMPORARY RULES (सभी को एक्सेस) - पहले इनसे शुरू करें
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
